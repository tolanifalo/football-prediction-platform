{
  "name": "@fpp/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint src drizzle.config.ts",
    "db:check": "node src/connection.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "node src/migrate.ts",
    "db:verify-generate": "node src/verify-generate.ts",
    "db:verify-harness": "node src/verify-harness.ts",
    "db:verify-partitions": "node src/verify-partitions.ts",
    "db:verify-canonical": "node src/verify-canonical.ts",
    "db:verify-external-ids": "node src/verify-external-ids.ts",
    "db:verify-fixtures": "node src/verify-fixtures.ts"
  },
  "dependencies": {
    "drizzle-orm": "0.45.2",
    "postgres": "^3.4"
  },
  "devDependencies": {
    "drizzle-kit": "0.31.10"
  }
}
